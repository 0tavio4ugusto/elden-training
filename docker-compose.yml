services:
  elden-training:
    build: .
    container_name: elden-training
    ports:
      - "${PORT:-3000}:80"
    volumes:
      - elden-data:/app/data
    restart: unless-stopped
    labels:
      - "app=elden-training"
      - "version=1.1"

volumes:
  elden-data:
